import * as Styled from './Styled';

export default function TopBar() {
  return (
    <Styled.TopBarLayout>
      <Styled.TopBarBox>
        <Styled.TopBarMenu>
          <Styled.TopBarMenuItem>로그인</Styled.TopBarMenuItem>
        </Styled.TopBarMenu>
      </Styled.TopBarBox>
    </Styled.TopBarLayout>
  );
}
