import { Header, Nav, PageTemplate, Search } from '../../components';

const SearchContainer = () => {
  return (
    <PageTemplate>
      <Header />
      <Search />
      <Nav />
    </PageTemplate>
  );
};

export default SearchContainer;
