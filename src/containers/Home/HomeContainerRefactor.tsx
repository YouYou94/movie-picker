import { HeaderRefactor, PageTemplate } from '../../components';

const HomeContainer_ = () => {
  return (
    <PageTemplate>
      <HeaderRefactor />
    </PageTemplate>
  );
};

export default HomeContainer_;
